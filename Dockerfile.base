ARG centos=7.9.2009
FROM aursu/rpmbuild:${centos}-build

USER root
RUN yum -y install \
        clang \
        openssl-devel \
        systemd-devel \
        tcl \
    && yum clean all && rm -rf /var/cache/yum
